# lets create a draft of this config file. I want it to have several top level objects: "cross_collection_properties" for collection properties that will be included across all collections, "collection_properties" for properties that are specific to this collection, "cross_item_properties" for item properties that will be included across all items, "item_properties" for properties that are specific to the items in this collection, "read_write_paths" that will contain the information needed my collection creation scripts will need to read and write from my s3 bucket. "collection_asset_info" which will contain the information about the assets contained in a collection and its items.  Use the gfm collection scripts as an example of the data I would put into each object ai!
