# look at my ble.json files collection_properties object along with how my collections are being defined in the ble, ahps, and gfm scripts I've shared with you. I'm looking for keys that are common across collections to put inside an object in this json file so that I don't have to add them to my collection specific config files like ble.json. I want this json file to just define the key and the type of value expected to be in the key. This means things like "id" are cross_collection property and would be defined here to indicate that the field should exist within a specific collection along with what type the value should be. I am creating this to be able to use it with pydantic when I start the creation of a specific collection using the ABC in collection_creator.py. So first tell me if this is a good idea and if it is a good idea then suggest code changes ai!
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cross Collection Schema",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "dataset_name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "keywords": {
      "type": "array",
      "items": { "type": "string" }
    },
    "extent": {
      "type": "object",
      "properties": {
        "spatial": {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 4,
          "maxItems": 4
        },
        "temporal": {
          "type": "array",
          "items": {
            "anyOf": [
              { "type": "string", "format": "date-time" },
              { "type": "null" }
            ]
          },
          "minItems": 2,
          "maxItems": 2
        }
      },
      "required": ["spatial", "temporal"]
    },
    "license": {
      "type": "string"
    }
  },
  "required": ["id", "dataset_name", "description", "title", "keywords", "extent", "license"]
}
