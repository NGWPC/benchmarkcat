'''
Take in a relative, self contained catalog, set the root of the catalog to be the s3 uri of the catalog json, normalize all the paths to this s3 uri, then convert those paths to signed s3 urls    
'''
